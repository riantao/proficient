import { libsServicesCustomers } from './libs-services-customers';

describe('libsServicesCustomers', () => {
  it('should work', () => {
    expect(libsServicesCustomers()).toEqual('libs-services-customers');
  });
});
