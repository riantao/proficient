import { libsServicesUsers } from './libs-services-users';

describe('libsServicesUsers', () => {
  it('should work', () => {
    expect(libsServicesUsers()).toEqual('libs-services-users');
  });
});
